import { View } from "../lib/types";

const nav: { label: string; view: View; icon: string }[] = [{ label: "Dashboard", view: "dashboard", icon: "▦" }, { label: "Empresas", view: "companies", icon: "▥" }, { label: "Historial de archivos", view: "history", icon: "◫" }];

export function Sidebar({ view, onChange, onLogout, user }: { view: View; onChange: (view: View) => void; onLogout: () => void; user?: { name: string; email: string } | null }) { return <aside className="sidebar"><div className="brand"><span className="brand-mark">JK</span><div><b>Estudio JK</b><small>PANEL ADMIN</small></div></div><div className="side-group"><span className="side-label">GESTIÓN</span>{nav.map((item) => <button key={item.view} className={`side-link ${view === item.view ? "active" : ""}`} onClick={() => onChange(item.view)}><span>{item.icon}</span>{item.label}</button>)}</div><div className="side-group automation"><span className="side-label">AUTOMATIZACIÓN</span><button className={`side-link ${view === "templates" ? "active" : ""}`} onClick={() => onChange("templates")}><span>↥</span>Generar plantillas</button><button className={`side-link ${view === "fees" ? "active" : ""}`} onClick={() => onChange("fees")}><span>▣</span>Generar honorarios</button></div><div className="sidebar-spacer"/><div className="side-user"><span className="avatar">{user?.name?.slice(0, 1) ?? "J"}</span><div><b>{user?.name ?? "JK — Administrador"}</b><small>{user?.email ?? "admin@estudiojk.com.pe"}</small></div></div><button className="logout" onClick={onLogout}>↪ <span>Cerrar sesión</span></button></aside>; }
